int indx=0;
int vol;
int len;
int x;

int space[] = {0, 0, 0, 0, 0};
int h[] = {0, 1, 0, 0, 0};
int e[] = {0, 0, 1, 0, 1};
int l[] = {0, 1, 1, 0, 0};
int o[] = {0, 1, 1, 1, 1};
int w[] = {1, 0, 1, 1, 1};
int r[] = {0, 1, 0, 1, 1};
int d[] = {0, 0, 1, 0, 0};

int msg[] = {0,0,0,0,0};

// ArrayBuild(msg, h, e);

len = ArrayLen (msg[]);


task main(){

//int len = 0;

while (indx < len+1)
      {
       ClearLine(LCD_LINE1);
       ClearLine(LCD_LINE3);
       NumOut(0, LCD_LINE1, msg[len]);
       NumOut(0, LCD_LINE3, len);
       Wait(2000);
       indx ++;
      }

Wait(5000);
}
